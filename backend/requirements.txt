fastapi==0.139.0
# EmailStr in the auth models needs this. fastapi 0.111 shipped it implicitly
# (all-extras era); slim fastapi >= 0.112 does not — without the explicit dep
# the app crashes on boot.
email-validator==2.3.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
gtts>=2.5.4
edge-tts>=7.2.8
ebooklib==0.20
mobi==0.4.1
beautifulsoup4==4.15.0
lxml>=6.1.1
supabase==2.31.0
pydantic-settings==2.14.2
mutagen==1.48.1
aiofiles==25.1.0
httpx==0.28.1
PyMuPDF==1.28.0
pdf2image==1.17.0
pytesseract==0.3.13
# Cover optimization (resize + WebP re-encode in app/services/image_service.py).
# Already a transitive dep of pdf2image/pytesseract; pinned because we now
# import it directly.
Pillow>=10.0

python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (latest release, unmaintained) reads bcrypt.__about__, which was
# removed in bcrypt >= 4.1 — bumping bcrypt breaks password hashing at runtime.
# Keep 4.0.1 until passlib is replaced (e.g. pwdlib or direct bcrypt).
bcrypt==4.0.1
openai>=2.45.0
# Used by scripts/translate_chapters_claude.py (offline book-translation tooling).
anthropic>=0.118.0
